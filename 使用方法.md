# 使用方法（從零開始的完整指南）

> 這份文件假設你**完全沒接觸過 Dynamics 365 CRM / Power Automate / Business Central**。
> 跟著做即可在 Windows 11 + PowerShell 上把整個 Customer Success Automation Hub 啟動起來。
> 共分 18 小節，每一節結束後你會得到一個可以執行 / 觀察的成果。

---

## 1. 這個專案在做什麼？

**Customer Success Automation Hub** 是一個模擬「Microsoft Partner / MSP 顧問公司」幫客戶
做數位轉型時會用到的整合平台。它把以下 Microsoft 產品的概念串成一條龍：

- **Dynamics 365 Sales / CRM**：管理潛在客戶、商機、報價（Lead → Opportunity → Quote）。
- **Dynamics 365 Business Central**：企業 ERP，這裡示範把簽核完成的付款請求同步進 BC。
- **Power Automate**：把企業內常見的「當 X 發生時，自動做 Y」流程 DSL 化。
- **Dataverse / Audit**：所有事件留紀錄，符合 compliance 要求。
- **BPM 簽核**：Vendor / Employee / Travel 三類請款的多階簽核。
- **IT Operation 服務台**：跟蹤 ticket、severity、SLA。
- **AI Assistant**：模擬 Copilot 的四種能力（客戶摘要、下一步建議、會議記錄轉任務、風險解釋）。

每一個外部系統都有「mock」版本，**你不需要真實的 Microsoft 帳號就能整套跑起來**。
未來想接真實服務時，只要把 `.env` 裡的 `*_MODE` 從 `mock` 改成 `live`，再填入 OAuth 設定即可。

---

## 2. 為什麼這個專案適合應徵 MSP / Microsoft Partner？

這間公司提供 Microsoft Solution、Dynamics 365 BC、Dynamics CRM、Power Automate、RPA、BPM、
Hybrid Cloud、IT Operation、Security IT、E-commerce、AI 工具與數位轉型顧問。本專案直接展示：

| 公司服務領域 | 本專案展示 |
|--------------|------------|
| Microsoft Solution / Dynamics CRM | CRM Pipeline 頁、Lead-to-Cash 流程 |
| Dynamics 365 Business Central | BPM 簽核完成後 Sync to BC 的端到端流程 |
| Power Automate / RPA / BPM | JSON 驅動的 workflow engine + 三類簽核 |
| Hybrid Cloud / IT Operation | 雙軌 SQLite/Postgres、Docker Compose、SLA tickets |
| Security IT | docs/security-notes.md 列出 OAuth / Key Vault 升級路徑 |
| AI 工具 / 數位轉型顧問 | 可插拔 AI Assistant + Audit log 文件化 |

公司強調 **Customer / Communication / Change / Innovation**：

- **Customer**：所有 KPI 都是站在客戶角度（pipeline、健康分數、SLA）。
- **Communication**：每筆操作都有 audit log，能跟客戶清楚說明發生了什麼。
- **Change**：mock → live 切換有清楚 migration plan。
- **Innovation**：AI Assistant + workflow engine 是未來顧問服務的差異化點。

---

## 3. 為什麼這個專案適合 Dynamics 365 Business Central 系統開發工程師？

BC 工程師日常會做：

1. 設計與客戶 ERP / CRM 的整合（資料同步、API、認證）。
2. 撰寫 Power Automate / RPA 的自動化流程。
3. 調整 BPM 簽核邏輯。
4. 排查上線後的 IT operation 與 security 問題。
5. 把客戶需求文件化、教育訓練、面對 PM。

本專案完整對應：

- 模型 (`apps/api/app/models.py`) 與 Dataverse / BC 概念對齊。
- `apps/api/app/services/business_central_client.py` 註明真實 BC API endpoint。
- Workflow engine 對應 Power Automate 的 trigger / condition / action 概念。
- BPM 簽核完成後同步 BC，對應顧問日常做的 PO/AP 自動化。
- docs/ 下整套文件可直接拿去面試或 onboarding 新顧問。

---

## 4. 需要安裝什麼？

最少需要：

- **Windows 11**（其他 OS 也可，但本指南以 Windows + PowerShell 為主）
- **PowerShell 5.1+** 或 **Windows PowerShell**（內建）
- **Python 3.11+**：<https://www.python.org/downloads/>
  - 安裝時務必勾選 **Add Python to PATH**。
- **Node.js 20 LTS+**：<https://nodejs.org/>
- **Git**：<https://git-scm.com/>

可選：

- **Docker Desktop**：用 docker compose 一鍵跑（不裝也可以手動跑）。
- **VS Code** 或任何編輯器。

驗證安裝（在 PowerShell 內輸入）：

```powershell
python --version    # 應該是 3.11.x 或更新
node --version      # 應該是 v20.x 或更新
npm --version
git --version
```

---

## 5. 怎麼從 PowerShell 啟動？

### 5.1 取得專案

```powershell
cd C:\Users\Xu\D365
# 如果還沒有：
git clone https://github.com/xu323/Customer-Success-Automation-Hub.git
cd Customer-Success-Automation-Hub
```

### 5.2 複製環境檔

```powershell
Copy-Item .env.example .env
```

預設值已經可以直接跑（mock 模式 + SQLite）。

---

## 6. 如何啟動 backend（FastAPI）？

```powershell
cd apps\api
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

看到 `Application startup complete.` 就是 OK。
打開瀏覽器：<http://localhost:8000/health>，會看到：

```json
{ "status": "ok", "version": "0.1.0", "env": "development" }
```

---

## 7. 如何啟動 frontend（React + Vite）？

**另開一個 PowerShell 視窗**（讓 backend 留著繼續跑）：

```powershell
cd C:\Users\Xu\D365\Customer-Success-Automation-Hub\apps\web
npm install
npm run dev
```

預設 <http://localhost:5173>。瀏覽器打開後左側 sidebar 會看到 8 個頁面。

---

## 8. 用 Docker Compose 一次跑全部

確認 Docker Desktop 已啟動，回到專案根目錄：

```powershell
cd C:\Users\Xu\D365\Customer-Success-Automation-Hub
docker compose up --build
```

啟動完成後：

- API：<http://localhost:8000/docs>
- Web：<http://localhost:5173>
- DB：postgres on `localhost:5432`（user/pass: csah/csah）

要停止：在那個 PowerShell 視窗按 `Ctrl + C`，再跑 `docker compose down`。

---

## 9. 如何執行測試？

### Backend（pytest）

```powershell
cd apps\api
.\.venv\Scripts\Activate.ps1
pytest -q
```

預期看到 `9 passed in 3.x s`。

### Frontend（typecheck + build）

```powershell
cd apps\web
npm run typecheck
npm run build
```

### Smoke test（同時驗 backend + frontend 是否都活著）

```powershell
cd C:\Users\Xu\D365\Customer-Success-Automation-Hub
.\scripts\smoke-test.ps1
```

---

## 10. 如何看 API 文件？

Backend 啟動後：

- **Swagger UI**：<http://localhost:8000/docs>（可以直接點 endpoint 試）
- **ReDoc**：<http://localhost:8000/redoc>
- **靜態 markdown**：[docs/api.md](docs/api.md)

---

## 11. 如何操作前端？

打開 <http://localhost:5173>，左側 sidebar 順序：

1. **Executive Dashboard**：6 個 KPI 卡 + 最近事件。一定先來這裡，建立全局印象。
2. **CRM Pipeline**：上方表格新增 Lead，按 Qualify 會自動建立 Opportunity，
   下方 Kanban 拖動觀念（這裡是按 Mark won）。
3. **Customer Onboarding**：剛 Mark won 後會看到自動建立的 onboarding project。
4. **BPM Requests**：建一筆 VendorPayment，按 Submit → Approve（兩階）→ Sync to Business Central。
5. **Automation Flows**：可以手動觸發 workflow，看到完整 timeline。
6. **IT Operation**：SLA 倒數、resolve ticket。
7. **AI Assistant**：填 Account ID = 1（Contoso）按 Generate。
8. **Audit Logs**：所有以上動作都會留下事件。

---

## 12. 如何理解 CRM Lead-to-Cash 流程？

這是 Dynamics 365 Sales 的標準流程：

```
Lead (新名單)
   │ qualify
   ▼
Opportunity (商機，記錄金額/機率/預計成交日)
   │ create quote
   ▼
Quote (報價單)
   │ accepted
   ▼
Opportunity 階段 = Won  ➜  自動觸發 Customer Onboarding 流程
```

對應檔案：

- 模型：`apps/api/app/models.py` 中的 `Lead`、`Opportunity`、`Quote`。
- API：`apps/api/app/routers/crm.py`。
- UI：`apps/web/src/pages/CRMPage.tsx`。

---

## 13. 如何理解 BPM 流程？

```
Draft (草稿)
   │ submit
   ▼
Submitted (送審) — 多位簽核者依序審核
   │ approve x N
   ▼
Approved (簽核通過)
   │ sync-to-bc
   ▼
Completed (已寫入 Business Central mock)
```

任一階段被 Reject 會直接到 Rejected 狀態。

---

## 14. 如何理解 Power Automate mock workflow？

每個 workflow 是一個 JSON：

```json
{
  "name": "Won opportunity -> Onboarding project",
  "trigger": { "type": "event", "event": "opportunity.won" },
  "conditions": [{ "path": "amount", "op": ">=", "value": 50000 }],
  "actions": [
    { "type": "create_onboarding_project", "params": { "target_days": 60 } },
    { "type": "send_notification", "params": { "to": "delivery@partner.com" } }
  ]
}
```

詳細所有 action 與條件運算子請看 [docs/workflow-engine.md](docs/workflow-engine.md)。

---

## 15. 如何理解 Business Central mock sync？

當 BPM 請求被 Approve，按 **Sync to Business Central** 會：

1. 呼叫 `apps/api/app/services/business_central_client.py` 的 mock client。
2. 依 request_type 寫入 mock 帳本（VendorPayment / EmployeePayment / TravelRequest）。
3. 回傳一個 `BC-VP-xxxxxx` / `BC-EP-...` 的文件編號。
4. 寫一筆 audit log 並把 BPM 請求標記為 Completed。

真實服務會打到：

```
POST https://api.businesscentral.dynamics.com/v2.0/{tenantId}/{environment}/api/v2.0/companies({companyId})/paymentJournals
```

---

## 16. 未來如何串接真實 Dynamics 365 / Business Central / Power Automate？

1. 在 Microsoft Entra ID（Azure AD）建立 App Registration。
2. 取得 `tenantId`、`clientId`、`clientSecret`、grant API permission（Dataverse / BC）。
3. 把 `.env` 中：

```
DYNAMICS_CRM_MODE=live
BUSINESS_CENTRAL_MODE=live
POWER_AUTOMATE_MODE=live
DYNAMICS_TENANT_ID=...
DYNAMICS_CLIENT_ID=...
DYNAMICS_CLIENT_SECRET=...
DYNAMICS_RESOURCE_URL=https://yourorg.crm.dynamics.com
BC_TENANT_ID=...
BC_ENVIRONMENT=Production
BC_COMPANY_ID=...
BC_CLIENT_ID=...
BC_CLIENT_SECRET=...
POWER_AUTOMATE_FLOW_TRIGGER_URL=https://prod-xx.westus.logic.azure.com:443/...
```

4. 在 `apps/api/app/services/*_client.py` 把 `if mode != "mock": raise` 那段
   換成 `httpx.Client(...)` 真的呼叫 Microsoft API。

詳細 endpoint 與 OAuth flow 請看 [docs/research-notes.md](docs/research-notes.md)。

---

## 17. 如何推送到 GitHub？

第一次推送：

```powershell
cd C:\Users\Xu\D365\Customer-Success-Automation-Hub
git init
git branch -M main
git remote add origin https://github.com/xu323/Customer-Success-Automation-Hub.git
git add .
git commit -m "Initial customer success automation hub"
git push -u origin main
```

如果 remote 已存在：

```powershell
git remote set-url origin https://github.com/xu323/Customer-Success-Automation-Hub.git
git push -u origin main
```

如果 push 跳出登入 / token 視窗，請：

1. 到 GitHub <https://github.com/settings/tokens> 申請 **Personal Access Token (Classic)**。
2. 勾選 `repo` 權限。
3. 用該 token 取代密碼欄位。

---

## 18. 面試時如何介紹這個專案？

完整講稿請看 [docs/interview-guide.md](docs/interview-guide.md)。30 秒精要版：

> 我做了一個叫 Customer Success Automation Hub 的作品集，模擬 Microsoft Partner 顧問
> 幫客戶導入 Dynamics 365 CRM、Business Central、Power Automate 與 BPM 的整合平台。
> 後端是 FastAPI + SQLAlchemy，包含 CRM Lead-to-Cash、Customer Onboarding、BPM 簽核、
> JSON workflow engine、Business Central mock sync、IT Operation tickets、AI Assistant
> 與 audit log。前端是 React + TypeScript + Tailwind 的 Microsoft Partner 風格 dashboard。
> 整體支援 SQLite 與 PostgreSQL，docker compose 一鍵啟動，GitHub Actions CI 跑 lint / 測試 / build。
> 所有 Microsoft 連線器都有 mock 模式，不需要 tenant 就能 demo，但保留切換真實服務的設定。
